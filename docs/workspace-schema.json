{
  "$schema": "https://json-schema.org/draft/2020-12",
  "version": "1.6.0",
  "description": "Single Source of Truth for workspace-wide invariants. All dependent documents must match these values. Validated by scripts/validate-templates.ts at pre-commit.",
  "workflow": {
    "phases": {
      "canonical": [
        "0",
        "1-2",
        "3",
        "4",
        "5",
        "6"
      ],
      "count": 6,
      "pm_owned": [
        "0",
        "1-2",
        "5"
      ],
      "autonomous": [
        "3",
        "4",
        "6"
      ],
      "definitions": {
        "0": "Project Initiation",
        "1-2": "Planning & Architecture",
        "3": "Design Handoff",
        "4": "Execution",
        "5": "Lifecycle Finalization",
        "6": "Quality Assurance & Finalization"
      }
    }
  },
  "agent_tiers": {
    "pm": "high",
    "architect": "high",
    "auditor": "medium",
    "docs-writer": "medium",
    "automation-engineer": "low",
    "scaffolding-expert": "low",
    "lifecycle-manager": "medium",
    "security-expert": "medium"
  },
  "models": {
    "claude": {
      "high": "claude-opus-5-0",
      "medium": "claude-sonnet-5-0",
      "low": "claude-haiku-4-5"
    },
    "gemini": {
      "high": "gemini-3.1-pro",
      "medium": "gemini-3.8-flash",
      "low": "gemini-3.8-flash"
    },
    "antigravity": {
      "high": "gemini-3.1-pro",
      "medium": "gemini-3.8-flash",
      "low": "gemini-3.8-flash"
    },
    "gemini-cli": {
      "high": "gemini-3.1-pro",
      "medium": "gemini-3.8-flash",
      "low": "gemini-3.8-flash"
    },
    "codex": {
      "high": "gpt-5.6-sol",
      "medium": "gpt-5.6-terra",
      "low": "gpt-5.6-luna"
    }
  },
  "variant_lifecycle": {
    "description": "Governance rules for variant lifecycle transitions",
    "beta_engagement_criteria_required": true,
    "engagement_criteria_schema": {
      "evaluation_start": "YYYY-MM-DD",
      "evaluation_period_days": 14,
      "green_threshold": "description of success condition",
      "yellow_action": "action if no engagement within period",
      "red_trigger": "condition for immediate beta cancellation",
      "current_status": "green|yellow|red"
    }
  },
  "_notes": {
    "variant_specific": "Variant-specific phase names (e.g., co-security Phase 3 = Threat Modeling) are declared in each variant's variant.json under phases.phase3_name",
    "variant_agents": "Variant-specific agent tiers are declared in each variant's variant.json under agent_tiers"
  },
  "i18n": {
    "description": "Internationalization policy. locale_codes defines all language codes recognized as valid translation zones. Files inside <lang-code>/ or locales/<lang-code>/ directories are exempt from the English-only documentation rule.",
    "locale_codes": [
      "ko",
      "ja",
      "zh-CN",
      "zh-TW",
      "de",
      "es",
      "fr",
      "pt",
      "vi",
      "ms",
      "id",
      "th",
      "ru",
      "it",
      "ar",
      "en"
    ],
    "translation_zone_patterns": [
      "<lang-code>/",
      "locales/<lang-code>/"
    ],
    "note": "audit.ts and validate-md-language.ts must read locale_codes from this file — do NOT hardcode language codes in scripts."
  },
  "country_scoped_assets": {
    "description": "Skills, scripts, env keys, and variant asset directories whose function is specific to one country/region because they access that country's data systems (NOT language-based). Pruned from generated projects/L3 drafts at scaffold time unless the project's target country matches. Registry-only SSOT — do not mark skills in SKILL.md frontmatter. Never register language skills (translate etc.) here.",
    "skills": {
      "k-law": "KR",
      "k-dart": "KR",
      "k-kosis": "KR",
      "k-opendata": "KR",
      "k-ecos": "KR",
      "k-krx": "KR"
    },
    "scripts": {},
    "env": {
      "DART_API_KEY": "KR",
      "LAW_API_OC": "KR",
      "KOSIS_API_KEY": "KR",
      "DATA_GO_KR_API_KEY": "KR",
      "ECOS_API_KEY": "KR",
      "KRX_API_KEY": "KR"
    },
    "dirs": {
      "regulations/KR": "KR"
    }
  },
  "variant_scoped_skills": {
    "description": "Skills whose function is exclusive to ONE variant. Registry-only SSOT (owner-curated — heuristics cannot distinguish these from genuinely common skills). If such a skill is found in templates/common/skills/ it is a promotion mistake: validate-templates FAILs and upgrade-project prunes it from projects whose variant does not own it (skills/ + .claude/.gemini/.agents mirrors). sound-synth (co-game) leaked this way on 2026-08-06 into 10 of 11 projects.",
    "co-game": [
      "sound-synth"
    ],
    "co-design": [],
    "co-consult": [
      "mece-logic-auditor"
    ],
    "co-deck": [
      "presenter-mode"
    ],
    "co-security": [
      "sarif-exporter",
      "stride-threat-matrix"
    ],
    "co-develop": [
      "swe-solve"
    ]
  },
  "rootAllowlist": {
    "description": "Whitelist of files and directories permitted in the workspace root. Anything not in these lists triggers a stray-artifact audit failure.",
    "files": [
      ".git",
      "package.json",
      "bunfig.toml",
      "package-lock.json",
      "bun.lockb",
      "bun.lock",
      "tsconfig.json",
      ".gitignore",
      ".gitattributes",
      ".gitmodules",
      ".editorconfig",
      ".gitleaks.toml",
      "README.md",
      "README_ko.md",
      "README_es.md",
      "README_ja.md",
      "CLAUDE.md",
      "GEMINI.md",
      "CODEX.md",
      "AGENTS.md",
      "CHANGELOG.md",
      "CONSTITUTION.md",
      "SECURITY.md",
      "LICENSE",
      "NOTICES",
      ".mcp.json",
      ".sync_context.tmp",
      "propagation-map.json",
      "services.yaml",
      "fix.mjs",
      ".DS_Store",
      ".cursorrules",
      ".clauderules",
      ".ignore",
      "opencode.json"
    ],
    "dirs": [
      ".git",
      "scripts",
      "tests",
      "agents",
      "skills",
      "docs",
      "memory",
      "templates",
      "tickets",
      "schemas",
      ".claude",
      ".gemini",
      ".githooks",
      ".git",
      "node_modules",
      ".antigravitycli",
      ".codex",
      ".github",
      "Projects",
      ".pipeline-state",
      "co-develop",
      ".agent",
      ".agents",
      ".opencode",
      ".openchrome",
      ".playwright-mcp",
      ".understand-anything",
      "abap_coding_mig",
      "abap_vibe_coding",
      "abap_vibe_coding_mig",
      "abap_vibe_coding_plugin",
      "artifacts",
      "Crypto-Agent",
      "daniel-lightrag-mcp",
      "dart-mcp",
      "demo",
      "mcp-server-datahub",
      "notebooklm-mcp-cli",
      "PDF-Viewer",
      "Pricing-Mgmt-Simulation",
      "Product-Planning",
      "quickdl",
      "scratch",
      "vibing-steampunk"
    ]
  },
  "variant_extensions": {
    "description": "Optional top-level keys allowed in variant.json beyond the base contract. Validated by validate-templates.ts.",
    "_shared": {
      "_note": "Documentation-only cross-reference. $ref values here are NOT machine-validated JSON Schema refs. For standard JSON Schema, use $defs pattern instead.",
      "agent_manifest": {
        "description": "Agent roster metadata and optional pipeline configuration. Available in all variants.",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order",
          "retry_policy"
        ],
        "notes": "pipeline_order: omit or use [] for variants with parallel/free-form agent dispatch. retry_policy: declares on_fail routing and max_retries per agent. optional[]: agents that can be skipped by PM."
      },
      "country_config": {
        "description": "Country/jurisdiction profile configuration. supported[] lists ISO 3166-1 alpha-2 or well-known region codes (EU, ASEAN; pattern ^[A-Z]{2,4}$) for profiles shipped under profiles_dir. default MUST be null (region-neutral) — a non-null default region-locks the variant and triggers a validator warning.",
        "required_fields": [
          "profiles_dir",
          "supported"
        ],
        "notes": "profiles_dir is 'docs/countries' in all current variants. Each supported code must have <profiles_dir>/<CODE>.md with frontmatter code/name/status/last_verified. Profiles are advisory jurisdiction knowledge (loaded at engagement start), never auto-executed. Country != language — language policy remains i18n.locale_codes."
      }
    },
    "co-abap": {
      "agent_manifest": {
        "description": "SAP ABAP development pipeline with optional SAP module analysts and technical execution agents",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order"
        ],
        "notes": "pipeline_order defines the 6-step harness lifecycle. optional[] lists agents dispatched by PM as needed. Module analysts (SD/MM/FI/CO/PP/LE) activate on matching trigger keywords."
      },
      "skill_manifest": {
        "description": "Variant-specific skills manifest with ABAP and SAP module skills",
        "required_fields": [
          "variant_specific"
        ],
        "notes": "Each entry declares name, layer, used_by_agents, phases, and platform_parity."
      },
      "script_manifest": {
        "description": "Variant-specific scripts for VSP (VS Code ABAP) task execution and ABAP project lifecycle",
        "required_fields": [
          "variant_scripts_dir",
          "local",
          "external"
        ],
        "notes": "local[] lists variant-only scripts. external[] reserved for future third-party integrations."
      }
    },
    "co-consult": {
      "agent_manifest": {
        "$ref": "_shared.agent_manifest"
      }
    },
    "co-design": {
      "agent_manifest": {
        "$ref": "_shared.agent_manifest"
      }
    },
    "co-develop": {
      "agent_manifest": {
        "$ref": "_shared.agent_manifest"
      }
    },
    "co-export": {
      "agent_manifest": {
        "description": "Import/export trade consulting agent roster with flat dispatch and no strict sequential dependency",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order"
        ],
        "notes": "Agents dispatched by PM based on trade-consulting engagement phase. pipeline_order is empty for free-form dispatch."
      },
      "displayName": {
        "description": "Human-readable variant display name",
        "required_fields": [],
        "notes": "Used in UI and documentation rendering."
      },
      "agent_overrides": {
        "description": "Per-agent additive or replacing overrides with reason, review tracking, and affected aspects",
        "required_fields": [
          "type",
          "reason",
          "since",
          "reviewed_by",
          "overrides"
        ],
        "notes": "type: additive | replacing. overrides[]: list of aspects this override affects (agent-roster, governance-workflow, dispatch-protocol, etc.)."
      },
      "engagement_methodology": {
        "description": "Variant-specific engagement methodology description",
        "required_fields": [],
        "notes": "Free-text field describing the structured engagement phases and client sign-off gates."
      },
      "deliverable_template": {
        "description": "Standard deliverable template descriptions per engagement phase",
        "required_fields": [],
        "notes": "Free-text field listing deliverable types and their phase mapping."
      },
      "betaLifecycleSummary": {
        "description": "Beta variant lifecycle tracking fields",
        "required_fields": [
          "betaEngagements",
          "betaBugs",
          "betaAgeInMonths",
          "promotionEligible",
          "missingCriteria",
          "lastSynced"
        ]
      }
    },
    "co-hr": {
      "displayName": {
        "description": "Human-readable variant display name",
        "required_fields": [],
        "notes": "Used in UI and documentation rendering."
      },
      "engagement_methodology": {
        "description": "Variant-specific engagement methodology description",
        "required_fields": [],
        "notes": "Free-text field describing the structured engagement phases and client sign-off gates."
      },
      "agent_overrides": {
        "description": "Per-agent additive or replacing overrides with reason, review tracking, and affected aspects",
        "required_fields": [
          "type",
          "reason",
          "since",
          "reviewed_by",
          "overrides"
        ],
        "notes": "type: additive | replacing. overrides[]: list of aspects this override affects (agent-roster, governance-workflow, dispatch-protocol, etc.)."
      },
      "skill_manifest": {
        "description": "Variant-specific skills manifest (e.g., k-law for Korean labor law compliance)",
        "required_fields": [
          "variant_specific"
        ],
        "notes": "Each entry declares name, used_by_agents, and phases."
      },
      "phaseAComplete": {
        "description": "Flag indicating Phase A (prototype) completion status",
        "required_fields": [],
        "notes": "Boolean. true when all PROMOTION_CHECKLIST criteria have been met."
      },
      "promotionChecklist": {
        "description": "Path to the promotion checklist file",
        "required_fields": [],
        "notes": "Relative path from variant root to the PROMOTION_CHECKLIST.md file."
      },
      "betaLifecycleSummary": {
        "description": "Beta variant lifecycle tracking fields",
        "required_fields": [
          "betaEngagements",
          "betaBugs",
          "betaAgeInMonths",
          "promotionEligible",
          "missingCriteria",
          "lastSynced"
        ]
      }
    },
    "co-news": {
      "agent_manifest": {
        "description": "Business/finance newsroom agent roster with flat dispatch and fact-checker sign-off gate",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order"
        ],
        "notes": "Agents dispatched by PM based on newsroom workflow phase. Fact-checker sign-off gate before publish."
      },
      "displayName": {
        "description": "Human-readable variant display name",
        "required_fields": [],
        "notes": "Used in UI and documentation rendering."
      },
      "agent_overrides": {
        "description": "Per-agent additive or replacing overrides with reason, review tracking, and affected aspects",
        "required_fields": [
          "type",
          "reason",
          "since",
          "reviewed_by",
          "overrides"
        ],
        "notes": "type: additive | replacing. overrides[]: list of aspects this override affects."
      },
      "betaLifecycleSummary": {
        "description": "Beta variant lifecycle tracking fields",
        "required_fields": [
          "betaEngagements",
          "betaBugs",
          "betaAgeInMonths",
          "promotionEligible",
          "missingCriteria",
          "lastSynced"
        ]
      },
      "phaseAComplete": {
        "description": "Flag indicating Phase A (prototype) completion status",
        "required_fields": [],
        "notes": "Boolean. true when all PROMOTION_CHECKLIST criteria have been met."
      },
      "promotionChecklist": {
        "description": "Path to the promotion checklist file",
        "required_fields": [],
        "notes": "Relative path from variant root to the PROMOTION_CHECKLIST.md file."
      }
    },
    "co-work": {
      "agent_manifest": {
        "$ref": "_shared.agent_manifest"
      }
    },
    "co-security": {
      "agent_manifest": {
        "$ref": "_shared.agent_manifest"
      }
    },
    "co-game": {
      "agent_manifest": {
        "description": "Game development pipeline order with optional and skippable agents",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order"
        ],
        "notes": "pipeline_order defines the sequential game development workflow. optional[] lists agents that can be omitted depending on genre (arcade, puzzle, etc.)."
      },
      "betaLifecycleSummary": {
        "description": "Beta variant lifecycle tracking fields",
        "required_fields": [
          "betaEngagements",
          "betaBugs",
          "betaAgeInMonths",
          "promotionEligible",
          "missingCriteria",
          "lastSynced"
        ]
      }
    },
    "co-deck": {
      "agent_manifest": {
        "description": "Strict pipeline order, optional flags, skippable flags, and retry policy for lecture workflow agents",
        "required_fields": [
          "variant_agents_dir",
          "optional",
          "notes"
        ],
        "optional_fields": [
          "pipeline_order",
          "retry_policy"
        ],
        "sub_fields": {
          "retry_policy": {
            "description": "Per-agent failure routing. on_fail names the agent to return to. max_retries caps loops. failure_condition is an expression evaluated by PM.",
            "example": {
              "source-verifier": {
                "on_fail": "research",
                "max_retries": 2,
                "failure_condition": "trust_score < 0.9 OR core_message_source_failed"
              }
            }
          },
          "optional": "Agent names that can be entirely omitted from the workflow run.",
          "skippable": "Agent names that run but whose failure is non-blocking (--skip-verify pattern)."
        },
        "notes": "optional[] = omit entirely. skippable[] = run but ignore failure. retry_policy = fail-and-loop-back."
      },
      "theme_manifest": {
        "description": "HTML theme definitions for lecture slides",
        "required_fields": [
          "themes_dir",
          "base_css",
          "available",
          "default",
          "overrides_dir"
        ],
        "notes": "CSS variable override themes. DOM structure immutable across all themes. overrides_dir accepts either on-disk layout per theme: flat (<overrides_dir>/<theme>.css) or nested (<overrides_dir>/<theme>/style.css, e.g. co-deck's shared styles/ pool) — validate-templates.ts B-04 checks both before failing."
      },
      "lecture_profile": {
        "description": "Lecture profile template metadata",
        "required_fields": [
          "template_path",
          "required_fields"
        ],
        "notes": "Scaffolded on new-project creation. Agents load this file at stage start."
      }
    }
  },
  "designLint": {
    "enabled": true,
    "scanRoots": [
      "templates/co-design/playground/src"
    ],
    "allowlist": [],
    "description": "Design-lint gate config (audit.ts checkDesignLint): token SSOT compliance scan roots for UI source; allowlist entries exempt documented literals."
  }
}
