# .env.sample — copy to .env and fill in values

# 국가법령정보센터 Open API OC 키
# 발급: https://www.law.go.kr/LSO/openApi/openApiOcPage.do
# Used by: k-law skill + scripts/migrate-registry-to-coordinates.ts
LAW_API_OC=your_oc_key_here

# NOTE (2026-08-26): GITHUB_TOKEN block removed — its only consumer was the
# legalize_kr MCP server (precedent search), deleted 2026-08-26. Existing
# users may safely drop GITHUB_TOKEN from their .env.
